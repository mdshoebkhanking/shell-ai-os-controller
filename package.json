{
  "name": "shell-ai-os-controller",
  "version": "1.0.13",
  "description": "AI-native desktop control layer for voice, tools, automation, agents, and local workflows.",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "test": "python -m pytest -q",
    "audit:repo": "python tools/repo_audit.py",
    "package": "python tools/package_public_release.py",
    "profile:memory": "python tools/memory_probe.py --ui --tts",
    "validate:release": "python tools/full_validation.py",
    "validate:prepush": "python tools/full_validation.py --require-clean-git"
  }
}
